package com.example.bookShop.models;

public class Admin extends User {
    public Admin(int id, String login, String password, double balance) {
        super(id, login, password, balance);
    }
}
